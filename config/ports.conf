# ============================================================
# NetRecon — Port Configuration
# ============================================================
# Define port lists used by different scan modules.
# Modify these to match your engagement needs.
# ============================================================

# --- Network Infrastructure Management Ports ---
# SSH, Telnet, TACACS+, DNS, DHCP, TFTP, HTTP/S, NTP, SNMP,
# BGP, LDAP, Syslog, NETCONF, RADIUS, HSRP, Cisco SCCP,
# Smart Install, SIP, OpenFlow, alt HTTP/S
INFRA_TCP_PORTS="22,23,49,53,67,68,69,80,123,161,162,179,389,443,514,830,1812,1813,1985,2000,4786,5060,6653,8080,8443,8888"

# --- UDP Critical Ports ---
# DNS, DHCP, TFTP, NTP, SNMP, IKE/VPN, Syslog, UPnP, NAT-T
CRITICAL_UDP_PORTS="53,67,68,69,123,161,162,500,514,1900,4500"

# --- Host Discovery Ports (TCP SYN probes) ---
DISCOVERY_TCP_PORTS="22,23,80,443,8080,8443"

# --- Host Discovery Ports (UDP probes) ---
DISCOVERY_UDP_PORTS="53,161,162"

# --- Web Management Ports ---
WEB_PORTS="80,443,8080,8443,8888,9090"

# --- SSL/TLS Check Ports ---
SSL_PORTS="443,8443,993,995,636,989,990"
